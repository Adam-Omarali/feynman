export default function CourseList(props){
    return(
        props.courses.map(course => {
            return(
                <div key={1}>
                    Hi
                </div>
            )
        }
    )
    )
}